const initialState = {
    userDetails: {
        id: 1,
        color: ''
    },
    loading: false,
    error: ''
};

const reducer = (state = initialState, action) => {
    return state;
};

export default reducer;
