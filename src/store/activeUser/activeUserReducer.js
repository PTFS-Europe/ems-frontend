const initialState = {
    userDetails: {
        id: 1,
        color: '',
        role_id: 14
    },
    loading: false,
    error: ''
};

const reducer = (state = initialState, action) => {
    return state;
};

export default reducer;
