const initialState = {
    userDetails: {
        id: 1,
        role_id: 14,
        name: 'Joe Customer'
    },
    loading: false,
    error: ''
};

const reducer = (state = initialState, action) => {
    return state;
};

export default reducer;
